# Android text pad exercise

This is a simple test app exercise. There are 2 branches:

main - Uses a Recycler view to display notes.
listview - Uses a ListView to display notes.

## Run tests

./gradlew connectedAndroidTest
